from fastapi import APIRouter

api_router = APIRouter()

# Routes will be added here by the generator
# from app.api.v1.endpoints import [domain]
# api_router.include_router([domain].router, prefix="/[domain]", tags=["[domain]"])